const packageTypes: string[] = ['Pay Per View'];
const packagePostTypes: string[] = ['All', 'Video', 'Audio'];

const packagesConfig = { packageTypes, packagePostTypes };

export default packagesConfig;
